# 🧪 คู่มือทดสอบ RUNING App - Solo Testing Guide

## เป้าหมาย
ทดสอบแอพด้วยตัวคนเดียว โดยมี 2 อุปกรณ์ (หรือ 2 เบราว์เซอร์):
- **อุปกรณ์ 1**: เป็น **Restaurant/Customer** (สร้าง order)
- **อุปกรณ์ 2**: เป็น **Rider** (รับงานและส่ง)

---

## 📋 Pre-Testing Checklist

### ✅ ขั้นตอนที่ 1: รัน SQL Scripts ทั้งหมด

เปิด **Supabase Dashboard → SQL Editor** แล้วรันไฟล์เหล่านี้ตามลำดับ:

```sql
-- 1. แก้ตาราง jobs (สำคัญมาก!)
รัน: FIX_JOBS_FINAL.sql

-- 2. สร้างตาราง reviews
รัน: supabase_reviews_setup.sql

-- 3. สร้าง storage bucket สำหรับรูป POD
รัน: supabase_storage_setup.sql
```

### ✅ ขั้นตอนที่ 2: ตรวจสอบว่า Dev Server กำลังรัน

```bash
npm run dev:https
# ควรเห็น: Local: https://192.168.x.x:5173
```

**สำคัญ**: ต้องใช้ **HTTPS** เท่านั้น (GPS บน mobile จะทำงาน)

### ✅ ขั้นตอนที่ 3: หา Local IP ของคุณ

```powershell
# Windows
ipconfig

# หา IPv4 Address (เช่น 192.168.1.100)
```

**URL ที่ใช้**: `https://192.168.x.x:5173` (เปลี่ยน x.x ให้ตรงกับ IP ของคุณ)

---

## 🎭 ขั้นตอนที่ 4: สร้าง 2 Accounts

### Account 1: Restaurant/Customer
1. เปิดเบราว์เซอร์ที่ 1 (เช่น Chrome)
2. ไปที่ `https://192.168.x.x:5173`
3. กด **Sign Up**
4. Email: `restaurant@test.com` / Password: `password123`
5. เลือก Role: **"ผมเป็นเจ้าของร้าน (Restaurant)"**

### Account 2: Rider (อุปกรณ์ที่ 2)
1. เปิดเบราว์เซอร์ที่ 2 (เช่น Firefox หรือโทรศัพท์)
2. ไปที่ `https://192.168.x.x:5173`
3. กด **Sign Up**
4. Email: `rider@test.com` / Password: `password123`
5. เลือก Role: **"ผมเป็นไรเดอร์ (Rider)"**

---

## 🧪 Test Scenario 1: สร้าง Order และส่งของ

### ฝั่ง Restaurant (อุปกรณ์ 1)

**Step 1: สร้าง Order**
1. ไปที่หน้า "สร้างงาน" (Create Job)
2. กรอกข้อมูล:
   - **ชื่อลูกค้า**: `ทดสอบ`
   - **ที่อยู่จัดส่ง**: `Bangkok, Sukhumvit Road`
   - **ราคา**: `100`
   - **รายการสินค้า**: `ข้าวผัด, น้ำ` (ถ้ามี input field)
3. **กด "📍 กดเพื่อระบุตำแหน่งร้าน"** เพื่อเปิด GPS
4. ยอมรับ Location Permission
5. กด **"สร้างออเดอร์"**

**ผลลัพธ์ที่คาดหวัง**:
✅ ขึ้น alert "สร้างออเดอร์สำเร็จ!"
✅ เห็น order ใหม่ในรายการ (status: `PENDING`)

---

### ฝั่ง Rider (อุปกรณ์ 2)

**Step 2: Accept Order**
1. ไปที่หน้า "งานพร้อมรับ" (Available Jobs)
2. เห็น order ที่เพิ่งสร้าง (ชื่อลูกค้า: `ทดสอบ`)
3. กด **"รับงาน"**

**ผลลัพธ์ที่คาดหวัง**:
✅ Order หายจากรายการ Available Jobs
✅ ย้ายไปที่ "งานของฉัน" (My Jobs)
✅ Status เปลี่ยนเป็น `ACCEPTED`

**Step 3: เปิด GPS**
1. อยู่ในหน้า Rider Dashboard
2. กด **"📍 เปิด GPS"**
3. ยอมรับ Location Permission

**ผลลัพธ์ที่คาดหวัง**:
✅ ปุ่มเปลี่ยนเป็น "GPS เปิดอยู่ ✅"
✅ GPS กำลังส่งตำแหน่งแบบ real-time

**Step 4: Update Status**
1. เปิด order ใน "งานของฉัน"
2. กด **"รับของแล้ว" (PICKED_UP)**
3. (ถ้าต้องการ) กด **"กำลังส่ง" (DELIVERING)**

**Step 5: อัปโหลดรูป POD และเสร็จงาน**
1. กด **"Choose file"** แล้วเลือกรูปจากเครื่อง
2. Preview รูปปรากฏ
3. กด **"งานเสร็จสิ้น" (DELIVERED)**

**ผลลัพธ์ที่คาดหวัง**:
✅ ขึ้น alert ว่า "ส่งของเสร็จแล้ว!"
✅ Wallet ของ rider เพิ่มเงิน (80% ของราคา = ฿80)
✅ Wallet ของ restaurant ลดเงิน (ราคาเต็ม = ฿100)
✅ Status เปลี่ยนเป็น `DELIVERED`

---

### กลับไปฝั่ง Customer (อุปกรณ์ 1)

**Step 6: ตรวจสอบ Order**
1. Logout จาก Restaurant account
2. Login ด้วย email: `customer@test.com` (หรือสร้างใหม่)
3. เลือก Role: **"ฉันคือลูกค้า (Customer)"**
4. ดูใน Customer Dashboard

**ผลลัพธ์ที่คาดหวัง**:
✅ เห็น order ที่สั่ง (ต้อง match ชื่อลูกค้า)
✅ Status: `DELIVERED`
✅ กดปุ่ม **"ดูรายละเอียด"** → เห็น OrderTrackingPage (Timeline)
✅ กดปุ่ม **"รีวิว"** → เห็นหน้ารีวิว

**Step 7: ส่งรีวิว**
1. ให้คะแนน 5 ดาว
2. เขียนความคิดเห็น: `"ส่งไวมาก บริการดี!"`
3. (Optional) อัปโหลดรูป
4. กด **"ส่งรีวิว"**

**ผลลัพธ์ที่คาดหวัง**:
✅ ขึ้น Success animation
✅ กลับไปหน้า List
✅ รีวิวถูกบันทึกในฐานข้อมูล

---

## 🧪 Test Scenario 2: Live GPS Tracking (ถ้ามีโทรศัพท์)

### Setup
- **อุปกรณ์ 1** (คอมพิวเตอร์): เปิดเป็น Customer
- **อุปกรณ์ 2** (โทรศัพท์): เปิดเป็น Rider

### Steps
1. Customer สร้าง order
2. Rider (โทรศัพท์) รับงาน
3. Rider เปิด GPS
4. **Customer กดปุ่ม "ดูรายละเอียด"** → กดปุ่ม **"ดูตำแหน่งแบบเรียลไทม์"**
5. Rider เดินไปมา (หรือขับรถ)

**ผลลัพธ์ที่คาดหวัง**:
✅ แผนที่เต็มจอปรากฏ (LiveTrackingPage)
✅ เห็น Rider marker เคลื่อนที่แบบ real-time
✅ ETA อัปเดตอัตโนมัติ
✅ เส้นทางเปลี่ยนตามตำแหน่ง rider

---

## 🐛 Troubleshooting

### ปัญหา: ไม่เห็น Order ใน Rider Dashboard
**สาเหตุ**: 
- RLS policies ไม่ถูกต้อง
- Jobs table ยังไม่มี columns ที่จำเป็น

**วิธีแก้**:
```sql
-- รัน FIX_JOBS_FINAL.sql อีกครั้ง
-- ตรวจสอบ RLS policies:
SELECT * FROM pg_policies WHERE tablename = 'jobs';
```

### ปัญหา: GPS ไม่ทำงานบน Mobile
**สาเหตุ**: ไม่ได้ใช้ HTTPS

**วิธีแก้**:
1. ตรวจสอบว่าใช้ `npm run dev:https` (ไม่ใช่ `npm run dev`)
2. URL ต้องขึ้นต้นด้วย `https://` (ไม่ใช่ `http://`)
3. อยู่ใน network เดียวกัน (WiFi เดียวกัน)

### ปัญหา: สร้าง Order ไม่ได้ (Database Error)
**สาเหตุ**: Table schema ไม่ตรง

**วิธีแก้**:
```sql
-- ตรวจสอบว่ามีคอลัมน์เหล่านี้หรือไม่:
SELECT column_name FROM information_schema.columns 
WHERE table_name = 'jobs' 
AND column_name IN ('rider_id', 'customer_name', 'items', 'category', 'proof_photo_url');

-- ควรได้ผลลัพธ์ครบ 5 columns
```

---

## 📊 Testing Checklist

- [ ] ✅ รัน SQL scripts ครบทั้งหมด (3 files)
- [ ] ✅ Dev server รันด้วย HTTPS
- [ ] ✅ สร้าง 2 accounts (Restaurant + Rider)
- [ ] ✅ Restaurant สร้าง order ได้
- [ ] ✅ GPS ทำงาน (ทั้ง Restaurant และ Rider)
- [ ] ✅ Rider เห็น available jobs
- [ ] ✅ Rider accept job ได้
- [ ] ✅ Rider update status ได้ (PICKED_UP, DELIVERING)
- [ ] ✅ Rider อัปโหลดรูป POD ได้
- [ ] ✅ Complete job แล้ว wallet เปลี่ยน
- [ ] ✅ Customer เห็น order status real-time
- [ ] ✅ OrderTrackingPage แสดงถูกต้อง (Timeline)
- [ ] ✅ LiveTrackingPage แสดงแผนที่และ rider marker
- [ ] ✅ Order review ส่งได้
- [ ] ✅ Chat ทำงาน (ระหว่าง Customer และ Rider)

---

## 🎯 Quick Start (TL;DR)

1. **รัน SQL scripts** (3 files) ใน Supabase
2. **เริ่ม dev server**: `npm run dev:https`
3. **สร้าง 2 accounts**: `restaurant@test.com` และ `rider@test.com`
4. **Restaurant**: สร้าง order → เปิด GPS
5. **Rider**: รับงาน → เปิด GPS → อัปโหลดรูป POD → เสร็จงาน
6. **Customer**: ดูรายละเอียด → ดู Live Tracking → ส่งรีวิว

---

## 💡 Pro Tips

1. **ใช้ 2 เบราว์เซอร์ต่างกัน** (Chrome + Firefox) เพื่อไม่ต้อง logout/login บ่อย
2. **Mobile Testing**: ใช้โทรศัพท์เป็น Rider เพื่อทดสอบ GPS แบบเดินจริง
3. **Network**: อุปกรณ์ทั้งหมดต้องอยู่ใน WiFi เดียวกัน
4. **HTTPS Warning**: เบราว์เซอร์จะแจ้งเตือน "Not Secure" → กด "Advanced" → "Proceed" (ปกติสำหรับ dev)

**พร้อมทดสอบแล้ว!** 🚀
